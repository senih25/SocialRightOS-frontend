# M2.0F — Rollback Runbook ve Uygulama Kanıtı

## Tetikleyici

Deployment yanıtı, açık `--target preview` komutuna rağmen `target: production` bildirdi. Bu, görev sınırı ihlali olduğundan derhal rollback tetiklendi.

## Uygulanan rollback

1. Yeni deploy veya promote işlemi durduruldu.
2. Geçici `socialrightos-frontend-preview` Vercel projesi silindi.
3. Projeye ait tüm deployment'ların silindiği CLI tarafından doğrulandı.
4. Deployment URL'si yeniden çağrıldı: HTTP `404`.
5. Proje listesi yeniden okundu: geçici proje yok.
6. Yerel `.vercel` bağlantısı güvenli biçimde kaldırıldı.
7. Repo worktree kontrol edildi: generated config/artifact kalmadı.

## Son durum

- Active preview deployment: 0
- Active production deployment: 0
- Custom domain/DNS change: 0
- Local Vercel link: removed
- Worktree pollution: 0

## Tekrar deneme kapısı

Tekrar deneme için ayrı insan onayı, önceden var olan açık bir preview-only proje veya target davranışını doğrulayan Vercel ayarı ve deploy öncesi proje-level target kanıtı gerekir. `--prod`, `promote`, domain veya DNS komutu kullanılmamalıdır.

## M2.1 rollback re-verification

- İzole M2.1 project removal: PASS
- Project lookup: 404
- Deployment lookup: 404
- Deployment URL: HTTP 404
- Local `.vercel` link: removed
- Active deployment count: 0
- Second deployment attempt: 0
