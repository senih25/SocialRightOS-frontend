# M2.0F — Smoke Test Report

## Dış preview

`BLOCKED_BY_DEPLOYMENT_TARGET_MISMATCH`. Dış URL, production-target sapması görüldüğü anda smoke için kullanılmadı ve rollback sonrası HTTP 404 verdi.

## Yerel production artifact smoke

Next standalone production server üzerinde iki user-agent profili kullanıldı:

- Desktop smoke user-agent
- iPhone/Mobile smoke user-agent

Her iki profilde aşağıdaki route'lar HTTP 200 ve boş olmayan HTML döndürdü:

- `/`
- `/start`
- `/gss-gelir-testi`
- `/65-yas-ayligi-uygunluk-testi`
- `/gizlilik-ve-kvkk`
- `/kullanim-kosullari`
- `/kaynak-ve-guncellik-politikasi`

## Sentetik pilot API smoke

- TR_GSS: HTTP 200, `X-Local-Fallback: 1`, deterministik `NEEDS_INFO`.
- TR_OLD_AGE_PENSION: HTTP 200, `X-Local-Fallback: 1`, deterministik `NEEDS_INFO`.
- Yalnız `synthetic-m2f-*` request kimlikleri ve kurgusal sayısal girdiler kullanıldı.
- Gerçek/kişisel/özel nitelikli veri kullanılmadı.

## Indexing ve analytics

- Yerel/non-production robots: `Disallow: /` doğrulandı.
- Sitemap endpoint'i güvenli boş/non-production üretim stratejisini korudu.
- Analytics minimizasyonu ve secret scan M2.0E test/CI kapılarından geçti.

## Sınır

Bu sonuç HTTP/render ve sentetik akış smoke kanıtıdır; dış preview üzerinde gerçek browser görsel, klavye ve screen-reader smoke yerine geçmez.

## M2.1 recovery

Target gate production sonucu verdiği için dış smoke yeniden çalıştırılmadı. `EXTERNAL_SMOKE_STATUS=NOT_EXECUTED_TARGET_GATE_FAILED`.
